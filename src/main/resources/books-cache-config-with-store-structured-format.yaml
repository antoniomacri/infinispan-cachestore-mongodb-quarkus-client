distributedCache:
  encoding:
    mediaType: "application/x-protostream"
  persistence:
    passivation: "false"
    mongodb-store:
      connection:
        uri: "mongodb://${env.MONGODB_HOST_PORT}/mydb"
        collection: "books"
      format: structured
